//package ex_05_03012025;

import io.qameta.allure.Description;
import org.openqa.selenium.By;
import org.openqa.selenium.PageLoadStrategy;
import org.openqa.selenium.edge.EdgeDriver;
import org.openqa.selenium.edge.EdgeOptions;
import org.testng.annotations.AfterTest;
import org.testng.annotations.BeforeTest;
import org.testng.annotations.Test;
import org.testng.thread.IThreadWorkerFactory;

import java.sql.Driver;

//public class Selenium24_SVG {
   // @BeforeTest
  //  public void openBrowser(){
    //    EdgeOptions options = new EdgeOptions();
      //  options.setPageLoadStrategy(PageLoadStrategy.NORMAL);
        //options.addArguments("--guest");
       // Driver = new EdgeDriver(options);

        //@Description("verify that the after search, result are visible");
      //  @Test
              //  public void test_flipcart_search(){
            //String URL "http//flipcart.com/";
           // Driver.get(URL);
         //   Driver.manage().window().maximize();

            //step 1- enter the macmini int the inputbox

       // Driver.findElement(By.name("q")).sendkeys();
       // List<Webelement>svgelement = Driver

           // @AfterTest
                //    public void closeBrowser() {
              //  try {
            //        Thread.sleep(3000);
          //      } catch (InterruptedException e) {
        //            throw new RuntimeException(e);
      //          }


    //        }

  //      }

//    }

//}
